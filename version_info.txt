# UTF-8
#
# version_info.txt — Windows VERSIONINFO resource for DobbyOS.exe (Mission 2).
# Consumed by dobby-desktop.spec via EXE(..., version=...). PyInstaller parses
# this file with eval() against its versioninfo classes (VSVersionInfo et al).
#
# Versioning: Mission 2 == 1.1 -> 1.1.0.0 for both file and product version.
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers/prodvers: (major, minor, patch, build) — must be four ints.
    filevers=(1, 1, 0, 0),
    prodvers=(1, 1, 0, 0),
    # mask/flags: all flag bits valid, none set (release build).
    mask=0x3f,
    flags=0x0,
    # OS=0x40004: designed for 32-bit Windows / Windows NT family.
    OS=0x40004,
    # fileType=0x1: VFT_APP (application).
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          '040904B0',  # U.S. English, Unicode (1200)
          [StringStruct('CompanyName', 'xicoocosta'),
           StringStruct('FileDescription', 'Dobby OS Desktop'),
           StringStruct('FileVersion', '1.1.0.0'),
           StringStruct('InternalName', 'DobbyOS'),
           StringStruct('LegalCopyright', 'AGPL-3.0-or-later'),
           StringStruct('OriginalFilename', 'DobbyOS.exe'),
           StringStruct('ProductName', 'Dobby OS Desktop'),
           StringStruct('ProductVersion', '1.1.0.0')])
      ]
    ),
    VarFileInfo([VarStruct('Translation', [1033, 1200])])
  ]
)
