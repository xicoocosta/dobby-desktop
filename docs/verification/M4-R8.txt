M4-R8 — absent-WebView2 branch harness (Mission 4c, VERIFIER)
Date: 2026-09-06  |  Repo HEAD: 15817966321ad6b5544b0c54fdfee5fef8497bc0
Machine: Windows 11 Pro 10.0.26100, x64. Real WebView2 Evergreen present
(HKLM\SOFTWARE\WOW6432Node\...\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5} pv=152.0.4191.66).

================================================================================
0. WHY THIS RUN EXISTS (carried gap from R7)
================================================================================
The shipped installer's absent-WebView2 branch — silent download of the
bootstrapper, Exec of it, registry RE-PROBE — had never executed anywhere:
WebView2 is present on every machine we have. R8 closes OUR code's side of
that gap with a fully LOCAL harness. Nothing was downloaded from the internet;
the only system-state change was the same per-user install/uninstall cycle
R3–R7 already performed. The shipped installer\dobby-desktop.iss was never
edited (git diff empty, proven in section 8).

================================================================================
1. HARNESS DESIGN (all under gitignored build\wv2-branch-test\)
================================================================================
  a) STUB EXE — 3-line C# compiled with the always-present .NET Framework
     compiler; stands in for MicrosoftEdgeWebView2Setup.exe. Accepts any
     args, exits 0. Served from build\wv2-branch-test\www\.
  b) LOCAL SERVER — repo .venv python, `-m http.server 8777 --bind
     127.0.0.1`, run from the www dir; its access log is independent proof
     the installer performed the download.
  c) TEST INSTALLER — byte-copy of installer\dobby-desktop.iss with SIX
     surgical, commented deviations (full diff in section 4):
       (1) new const TEST_FORCE_MISSING=True, applied ONLY at the initial
           detection call site in PrepareToInstall
           (`if (not TEST_FORCE_MISSING) and IsWebView2Installed then`) —
           the re-probe inside TryInstallWebView2 still calls the REAL
           IsWebView2Installed, untouched;
       (2) WV2BootstrapperUrl -> http://127.0.0.1:8777/MicrosoftEdgeWebView2Setup.exe;
       (3) OutputDir=. / OutputBaseFilename=DobbyOS-Setup-TEST (nothing
           lands in dist\release);
       (4) "TEST HARNESS — NEVER SHIP" header block;
       (5)(6) mechanical relative-path fixes for the file living two levels
           below repo root: LicenseFile=..\..\LICENSE; Files sources
           ..\..\dist\DobbyOS\*, ..\..\installer\INSTALL.txt,
           ..\SOURCE-NOTICE.txt.
  Pre-flight: ports 8777 and 7001 both free (Get-NetTCPConnection empty);
  no existing DobbyOS install (dir absent, uninstall key absent).

================================================================================
2. STUB BUILD (raw)
================================================================================
> C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe /nologo /target:exe
    /out:...\www\MicrosoftEdgeWebView2Setup.exe stub.cs
(no output)
csc exit: 0
stub size: 3584 bytes
manual run `MicrosoftEdgeWebView2Setup.exe /silent /install` -> stub exit: 0

stub.cs (verbatim):
    // R8 TEST STUB — stands in for MicrosoftEdgeWebView2Setup.exe. Accepts any args, exits 0.
    class Stub {
        static int Main(string[] args) { return 0; }
    }

================================================================================
3. LOCAL SERVER
================================================================================
Started: .venv\Scripts\python.exe -u -m http.server 8777 --bind 127.0.0.1
(cwd = build\wv2-branch-test\www, log -> build\wv2-branch-test\http-access.log)
Pre-flight HEAD probe returned 200, Content-Length: 3584.

================================================================================
4. TEST .ISS — THE COMPLETE DIFF vs THE SHIPPED FILE
================================================================================
git diff --no-index installer\dobby-desktop.iss build\wv2-branch-test\webview2-branch-test.iss
(6 hunks; every changed line shown)

@@ -1,3 +1,20 @@
+; ===========================================================================
+; TEST HARNESS — NEVER SHIP.  (Mission 4c / R8, 2026-09-06)
+; Copy of installer\dobby-desktop.iss with FOUR deliberate deviations so the
+; absent-WebView2 branch (download -> bootstrapper exec -> registry re-probe)
+; can be exercised on a machine that HAS WebView2:
+;   1. TEST_FORCE_MISSING forces the INITIAL detection to "missing"
+;      (the post-exec RE-PROBE still calls the real IsWebView2Installed).
+;   2. WV2BootstrapperUrl points at a LOCAL stub server (127.0.0.1:8777),
+;      never the internet.
+;   3. OutputBaseFilename/OutputDir -> DobbyOS-Setup-TEST in this harness dir
+;      (nothing lands in dist\release).
+;   4. Relative paths (LicenseFile, Files sources) adjusted for this file's
+;      location two levels below the repo root.
+; This file lives under gitignored build\ and must never be compiled for
+; distribution.
+; ===========================================================================
+;
 ; dobby-desktop.iss — Inno Setup script for the Dobby OS Desktop per-user installer.

@@ -44,7 +61,8 @@
 ; AGPL-3.0 text shown (and must be accepted) during install.
-LicenseFile=..\LICENSE
+; TEST HARNESS path adjustment: repo root is two levels up from here.
+LicenseFile=..\..\LICENSE

@@ -53,8 +71,9 @@
-OutputDir=..\dist\release
-OutputBaseFilename=DobbyOS-Setup-{#MyAppVersion}
+; TEST HARNESS: output stays in the harness dir — NOTHING lands in dist\release.
+OutputDir=.
+OutputBaseFilename=DobbyOS-Setup-TEST

@@ -64,11 +83,12 @@  [Files]
-Source: "..\dist\DobbyOS\*"; DestDir: "{app}"; Flags: recursesubdirs createallsubdirs ignoreversion
+; TEST HARNESS path adjustments: sources resolved from build\wv2-branch-test\.
+Source: "..\..\dist\DobbyOS\*"; DestDir: "{app}"; Flags: recursesubdirs createallsubdirs ignoreversion
-Source: "INSTALL.txt"; DestDir: "{app}"; Flags: ignoreversion
+Source: "..\..\installer\INSTALL.txt"; DestDir: "{app}"; Flags: ignoreversion
-Source: "..\build\SOURCE-NOTICE.txt"; DestDir: "{app}"; Flags: ignoreversion
+Source: "..\SOURCE-NOTICE.txt"; DestDir: "{app}"; Flags: ignoreversion

@@ -129,7 +149,13 @@  [Code] const
-  WV2BootstrapperUrl = 'https://go.microsoft.com/fwlink/p/?LinkId=2124703';
+  // TEST HARNESS: local stub server instead of Microsoft's permalink — the
+  // shipped value is 'https://go.microsoft.com/fwlink/p/?LinkId=2124703'.
+  WV2BootstrapperUrl = 'http://127.0.0.1:8777/MicrosoftEdgeWebView2Setup.exe';
+  // TEST HARNESS: forces the INITIAL detection decision (PrepareToInstall)
+  // to the "missing" branch. Used at that ONE call site only — the re-probe
+  // inside TryInstallWebView2 still calls the real IsWebView2Installed.
+  TEST_FORCE_MISSING = True;

@@ -271,7 +297,10 @@  PrepareToInstall
-  if IsWebView2Installed then
+  // TEST HARNESS: the shipped line is `if IsWebView2Installed then`. The
+  // `(not TEST_FORCE_MISSING) and` guard short-circuits ONLY this initial
+  // decision to "missing"; the re-probe in TryInstallWebView2 is untouched.
+  if (not TEST_FORCE_MISSING) and IsWebView2Installed then

(Full machine diff preserved at build\wv2-branch-test\iss-edits.diff, 5045 bytes.)

================================================================================
5. ISCC COMPILE (tail, raw)
================================================================================
> %LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe build\wv2-branch-test\webview2-branch-test.iss
Compiler engine version: Inno Setup 6.7.3
...
Compiling [Code] section
Creating setup files
   Verification successful
   Updating icons (Setup.exe)
   Updating version info (Setup.exe)
   Updating manifest (Setup.exe)

Successful compile (60.516 sec). Resulting Setup program filename is:
C:\Users\Admin\Documents\dobby-desktop\build\wv2-branch-test\DobbyOS-Setup-TEST.exe

ISCC exit: 0   (errors: 0)
Output exe: 83,297,462 bytes, in the harness dir — dist\release untouched.
(Full 396,071-byte compiler log at build\wv2-branch-test\iscc-output.txt.)

================================================================================
6. THE RUN — EVIDENCE CHAIN (all verbatim)
================================================================================
> DobbyOS-Setup-TEST.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART
    /LOG=build\wv2-branch-test\r8-install.log
install exit: 0

r8-install.log lines 1..19 (the branch under test, verbatim):
2026-09-06 21:31:28.155   Log opened. (Time zone: UTC+01:00)
2026-09-06 21:31:28.155   Setup version: Inno Setup version 6.7.3
2026-09-06 21:31:28.155   Original Setup EXE: C:\Users\Admin\Documents\dobby-desktop\build\wv2-branch-test\DobbyOS-Setup-TEST.exe
2026-09-06 21:31:28.155   Setup command line: /SL5="$80652,81957334,893952,C:\Users\Admin\Documents\dobby-desktop\build\wv2-branch-test\DobbyOS-Setup-TEST.exe" /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /LOG=C:\Users\Admin\Documents\dobby-desktop\build\wv2-branch-test\r8-install.log
2026-09-06 21:31:28.155   Windows version: 10.0.26100
2026-09-06 21:31:28.155   Windows architecture: x64 (64-bit)
2026-09-06 21:31:28.155   Machine types supported by system: x86 x64
2026-09-06 21:31:28.155   User privileges: None
2026-09-06 21:31:28.166   Administrative install mode: No
2026-09-06 21:31:28.166   Install mode root key: HKEY_CURRENT_USER
2026-09-06 21:31:28.166   64-bit install mode: Yes
2026-09-06 21:31:28.166   RedirectionGuard status for current process: Enabled in enforcing mode
2026-09-06 21:31:28.167   Created temporary directory: C:\Users\Admin\AppData\Local\Temp\is-C4GKFTG0L1.tmp
2026-09-06 21:31:28.386   WebView2: not detected — attempting Evergreen bootstrapper.
2026-09-06 21:31:28.386   Downloading temporary file from http://127.0.0.1:8777/MicrosoftEdgeWebView2Setup.exe: C:\Users\Admin\AppData\Local\Temp\is-C4GKFTG0L1.tmp\MicrosoftEdgeWebView2Setup.exe
2026-09-06 21:31:28.387   Download is not using basic authentication
2026-09-06 21:31:28.493   WebView2: bootstrapper finished with exit code 0.
2026-09-06 21:31:28.493   WebView2: per-machine (WOW6432Node) hit, pv=152.0.4191.66
2026-09-06 21:31:28.493   WebView2: runtime present after bootstrap step.

READING OF THE CHAIN:
  [miss]      "WebView2: not detected — attempting Evergreen bootstrapper."
              — the forced-missing branch entered. Note there is NO
              "hit, pv=" or "no usable pv value" line BEFORE it: the guard
              short-circuited, i.e. the initial probe really was bypassed
              rather than merely ignored.
  [download]  DownloadTemporaryFile fetched from 127.0.0.1:8777 into {tmp}
              (the silent, zero-UI path — WizardSilent branch).
  [server]    independent proof from http-access.log (verbatim, complete):
                  Serving HTTP on 127.0.0.1 port 8777 (http://127.0.0.1:8777/) ...
                  127.0.0.1 - - [06/Sep/2026 21:29:56] "HEAD /MicrosoftEdgeWebView2Setup.exe HTTP/1.1" 200 -
                  127.0.0.1 - - [06/Sep/2026 21:31:28] "GET /MicrosoftEdgeWebView2Setup.exe HTTP/1.1" 200 -
              (the HEAD 21:29:56 is our pre-flight probe; the GET 21:31:28
              is the installer — timestamp matches the log line above.)
  [exec]      "bootstrapper finished with exit code 0." — Exec ran the
              downloaded stub from {tmp} with '/silent /install',
              ewWaitUntilTerminated, and got its exit code.
  [re-probe]  "per-machine (WOW6432Node) hit, pv=152.0.4191.66" — this line
              is emitted INSIDE the real IsWebView2Installed, called by the
              re-probe seam in TryInstallWebView2 against the LIVE registry.
  [success]   "WebView2: runtime present after bootstrap step."
Then the normal install proceeded; log tail (verbatim):
2026-09-06 21:31:37.529   Installation process succeeded.
2026-09-06 21:31:37.529   Need to restart Windows? No
2026-09-06 21:31:37.532   Deinitializing Setup.
2026-09-06 21:31:37.540   Log closed.
(r8-install.log: 14,069 lines total, kept in the harness dir.)
Post-install checks: {localappdata}\Programs\DobbyOS\DobbyOS.exe present;
HKCU uninstall key {9E64D977-...}_is1 DisplayName "Dobby OS Desktop" 1.1.0.

================================================================================
7. CLEANUP
================================================================================
> unins000.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART
uninstall exit: 0
%LOCALAPPDATA%\Programs\DobbyOS : GONE (polled until absent)
HKCU uninstall key             : GONE
http server (PID 10508 python) : killed; port 8777 verified free again.

================================================================================
8. HYGIENE / REGRESSION
================================================================================
git status --porcelain : EMPTY before this evidence file was written
                         (harness lives entirely under gitignored build\).
git diff -- installer/dobby-desktop.iss : EMPTY — 0 lines. Shipped file untouched.
dist\release before vs after (SHA256 + size + mtime, byte-identical):
  C01FFBB7586A0AD7FC40E251DE3CFF6BEF9EE5DF5E6C0D0AF744549A126BE860  83305072  2026-09-05T13:46:40.6847807Z  DobbyOS-Setup-1.1.0.exe
  822CAD001BD986C5B89DA6CF59808F93530D52F35742B0369CD7B9C2D6AE423E      2710  2026-09-05T13:41:31.1090323Z  INSTALL.txt
  997D191F54243520295C607B1EE55DBBF96ABB402739B9FC395193E039506BA2       183  2026-09-05T13:46:41.5870192Z  SHA256SUMS.txt
  19061E5D3A47B8028DFB8200C84B98C0889BFEFA1048DED94D678C048F47CDAF  33826255  2026-09-05T13:45:45.2811333Z  SOURCE-DobbyOS-1.1.0.zip

================================================================================
9. VERDICT
================================================================================
PASS. The absent-WebView2 branch of OUR installer code executed end-to-end
for the first time, with every link of the chain evidenced from two
independent sides (installer log + server access log):
  miss decision -> silent download (200) -> bootstrapper Exec (exit 0) ->
  registry RE-PROBE via the REAL IsWebView2Installed (pv=152.0.4191.66) ->
  "runtime present after bootstrap step." -> install completes, exit 0.

HONEST SCOPE STATEMENT: this proves OUR download/exec/re-probe code executes
correctly when detection reports missing. It does NOT test Microsoft's real
bootstrapper (a stub stood in for it), nor a machine where WebView2 is truly
absent end-to-end (the re-probe here succeeded because the REAL runtime is
present on this machine — which is exactly what made the seam necessary).
Microsoft's bootstrapper behavior remains external and untested: their
product. The remaining residual risk is environmental (their exe, their
servers), not in our shipped code path.

Harness artifacts kept for re-runs (all gitignored):
  build\wv2-branch-test\{stub.cs, www\MicrosoftEdgeWebView2Setup.exe,
  webview2-branch-test.iss, iss-edits.diff, iscc-output.txt,
  DobbyOS-Setup-TEST.exe, r8-install.log, http-access.log,
  dist-release-baseline.txt}
